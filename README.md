
  # TPPv1.2

  This is a code bundle for TPPv1.2 The original project is available at figma 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  